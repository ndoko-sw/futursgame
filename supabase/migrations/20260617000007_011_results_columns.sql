-- Migration 011: Add missing columns to results table

ALTER TABLE results
  ADD COLUMN IF NOT EXISTS session_id UUID REFERENCES sessions(id) ON DELETE CASCADE,
  ADD COLUMN IF NOT EXISTS score_ventes NUMERIC(5,2) NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS score_image NUMERIC(5,2) NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS score_durabilite NUMERIC(5,2) NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS score_fidelite NUMERIC(5,2) NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS score_global NUMERIC(8,2) NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS budget_remaining INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS budget_next INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS event_id UUID REFERENCES market_events(id) ON DELETE SET NULL,
  ADD COLUMN IF NOT EXISTS event_ids TEXT[] DEFAULT '{}';

NOTIFY pgrst, 'reload schema';
